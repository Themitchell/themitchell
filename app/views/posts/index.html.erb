<% content_for :aside do %>
  <%= render 'shared/nav_category' %>
<% end %>

<% if @posts.any? %>
  <ol class="list">
    <%= render @posts %>
  </ol>
<% else %>
  <p>There are currently no posts.</p>
<% end %>

<%= paginate(@posts) %>
